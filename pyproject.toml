[project]
name = "lexaway-packs"
version = "0.1.0"
description = "Ingestion pipeline for Lexaway language packs"
requires-python = ">=3.13"
dependencies = [
    "httpx",
    "spacy>=3.8",
]

[dependency-groups]
dev = ["pytest"]
